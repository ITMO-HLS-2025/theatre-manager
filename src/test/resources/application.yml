spring:
  flyway:
    enabled: false
  cloud:
    config:
      enabled: false
    discovery:
      enabled: false
      client:
        simple:
          enabled: false
  config:
    import: "optional:configserver:"

eureka:
  client:
    enabled: false
    register-with-eureka: false
    fetch-registry: false
